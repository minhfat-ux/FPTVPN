# CLAUDE.md

Luật cho mọi agent trong repo này nằm ở **`AGENTS.md`** (gốc repo).
Đọc `AGENTS.md` trước khi sửa file. Tóm tắt: worker chỉ sửa file trong phạm vi brief,
**không** dùng git/ssh/deploy, không thêm dependency, không nới lỏng test, và phải báo cáo
theo mẫu `docs/templates/agentic-project/AGENT_HANDOFF.md` kèm bằng chứng lệnh đã chạy.
