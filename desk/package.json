{
  "name": "fbuddy-desk",
  "private": true,
  "version": "0.1.0",
  "description": "flowdesk — backend riêng cho app Windows (MeetFlow AI Overlay): cấp mã kích hoạt từ đơn đã trả tiền, proxy Soniox/OpenRouter, không bao giờ trả key ra client.",
  "type": "module",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test test/"
  },
  "dependencies": {
    "ws": "^8.21.3"
  }
}
