# UI/UX Task — PrivateVPN iOS (2026-08-19)

## Mục tiêu
Redesign SwiftUI UI theo target UX trong master spec §1:
`Open app → Sign in/authorize (GATE 4, chưa làm) → Choose Vietnam → Connect → Connected → Internet exits through Vietnam`.

## Files thay đổi

| File | Thay đổi |
|------|----------|
| `iOS/PrivateVPN/ContentView.swift` | Redesign toàn bộ: status card (màu + SF Symbol theo `VPNState`), 1 nút primary toggle Connect/Disconnect (NFR-UX-001 AC-022), location card (flag + tên server từ `VPNConfigStore.selectedLocation`/`serverEndpoint`), diagnostics card (FR-DIAG-001 AC-014: state, node, VPN IP = `tunnelAddress`, last error — không hiển thị private key/public key), dark gradient theme, error banner, hint khi chưa cấu hình. |
| `iOS/PrivateVPN/SettingsView.swift` | Polish theo theme mới: dark gradient nền, `.tint(VPNTheme.accent)`, footer an toàn ("private key never leaves device"), footer note GATE 4. Toàn bộ chức năng giữ nguyên (control plane URL/token, location picker, server/peer fields, device public key). |
| `iOS/PrivateVPN/Theme.swift` (MỚI) | `VPNTheme` (accent, gradient, card style) + UI-only extension `VPNState.symbol/.tint/.subtitle/.isTransitioning`. |
| `Vendor/WireGuardKit/Sources/WireGuardKitGo/Makefile` | Thêm `GOOS_iphonesimulator := ios` — **fix build infra bắt buộc** (xem bên dưới). |

Không đụng logic: `VPNManager.swift`, `VPNState.swift`, `VPNConfigStore.swift`, `KeychainStore.swift`, `WireGuardConfig.swift`, `ControlAPIClient.swift` — chỉ UI.

## Vấn đề gặp phải & cách xử lý

1. **Go upgrade 1.26.6 (18:13) làm vỡ simulator build** (gate1 build 16:23 SUCCEEDED với Go cũ):
   - `libwg-go.a` build với GOOS=darwin (Makefile cũ thiếu mapping `iphonesimulator`) → slice arm64 thiếu symbol `darwin_arm_init_mach_exception_handler` (Go 1.26 chỉ compile stub này khi GOOS=ios, file `gcc_signal_ios_nolldb.c`).
   - Fix: thêm `GOOS_iphonesimulator := ios` (đúng như upstream WireGuardKit) + xoá `wg/`/`wg-tmp/` cũ trong DerivedData để rebuild sạch. Đã verify goruntime patch (`boottime-over-monotonic.diff`) vẫn áp được lên Go 1.26.6.
2. **Swift type-check timeout** ở SettingsView (`LabeledContent` trailing closure + `onChange`): fix bằng `LabeledContent("Endpoint", value:)` và tách `applyLocation(_:)`.
3. **Parallel worker**: có agent khác đang build `generic/platform=iOS` và sửa `control-plane/*` (không đụng file của task này). Worker đó đồng thời refactor `SettingsView.swift` (tách sections thành computed properties + áp `applyLocation`) — nội dung theme/footer của task này được giữ nguyên trong file cuối (mtime 21:34). Cần kiểm tra trước khi commit.
4. `xcodebuild` đôi lúc fail "database is locked" do build song song — chờ worker kia xong rồi build lại.

## Kết quả build

Command:
```
xcodegen generate
xcodebuild -project PrivateVPN.xcodeproj -scheme PrivateVPN \
  -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build
```

Log đầy đủ: `evidence/builds/2026-08-19-uiux-build.log`

Excerpt cuối log:
```
** BUILD SUCCEEDED **
```
Exit code 0. Không warning ở `ContentView.swift`/`SettingsView.swift`/`Theme.swift`.
