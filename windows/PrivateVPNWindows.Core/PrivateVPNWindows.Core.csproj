<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <RootNamespace>VpnFlow.Core</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <LangVersion>latest</LangVersion>
  </PropertyGroup>

  <ItemGroup>
    <!-- Test chạy THẬT đoạn dò gateway qua PowerShell để bắt lỗi trích dẫn (xem RunPowerShellAsync). -->
    <InternalsVisibleTo Include="PrivateVPNWindows.Core.Tests" />
  </ItemGroup>

</Project>
